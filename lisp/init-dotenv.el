;;; init-dotenv.el --- A simple major mode to provide .env files with color highlighting -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(use-package dotenv-mode
  :defer t
  :ensure t
  :config)

(provide 'init-dotenv)
;;; init-dotenv.el ends here.
